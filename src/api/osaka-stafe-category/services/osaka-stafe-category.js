'use strict';

/**
 * osaka-stafe-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::osaka-stafe-category.osaka-stafe-category');
