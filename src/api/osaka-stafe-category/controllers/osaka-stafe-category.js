'use strict';

/**
 *  osaka-stafe-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::osaka-stafe-category.osaka-stafe-category');
